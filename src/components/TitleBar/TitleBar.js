import { StyledTitleBar, Title } from './TitleBar.styled';
const TitleBar = () => {
  return (
    <StyledTitleBar>
      <Title>Products</Title>
    </StyledTitleBar>
  );
};

export default TitleBar;
